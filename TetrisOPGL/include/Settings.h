#pragma once


const float maxTimer = 1.5f;
const float maxInputTimer = 0.1f;
const float levelTimerReduction = 0.25f;
const int baseLevelUpTreshold = 2500;
constexpr int tileHeight = 27; // taille de 20 plus les bordures inférieure et supérieure et la limite supérieur (20 + 2 + 4)
constexpr int tileWidth = 12; // taille de 10 plus les bordures droites et gauche
const int baseRowPoint = 100;
const float tileSize = 20.0f;
